# animemiru.jp 向け nginx リバースプロキシ
# SSL証明書は certbot 等で取得し deploy/certs/ に配置
# 正規ホスト: animemiru.jp（www なし）

upstream nextjs {
    server app:3000;
}

# HTTP → HTTPS（www は apex へ）
server {
    listen 80;
    server_name www.animemiru.jp;
    return 301 https://animemiru.jp$request_uri;
}

server {
    listen 80;
    server_name animemiru.jp;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://animemiru.jp$request_uri;
    }
}

# HTTPS www → apex
server {
    listen 443 ssl http2;
    server_name www.animemiru.jp;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    return 301 https://animemiru.jp$request_uri;
}

server {
    listen 443 ssl http2;
    server_name animemiru.jp;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    client_max_body_size 50M;

    # wp-content: 新 VPS の data/wp-content を優先、無ければ旧サーバーへフォールバック
    location /wp-content/ {
        root /var/www;
        try_files $uri @wp_content_legacy;
        expires 30d;
        add_header Cache-Control "public";
        access_log off;
    }

    location @wp_content_legacy {
        proxy_pass https://49.212.243.171;
        proxy_ssl_server_name on;
        proxy_set_header Host animemiru.jp;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }

    location /admin {
        proxy_pass http://nextjs;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /api {
        proxy_pass http://nextjs;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://nextjs;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
